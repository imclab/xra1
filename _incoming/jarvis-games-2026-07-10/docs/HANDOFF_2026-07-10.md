# Session handoff — 2026-07-10 (scale-template games + creator provenance)

**State: 🟢 loop 90/90, streak ×12.** Two generative games shipped in the scale-template framework; a 3-strand
provenance/minting research spike synthesized into a recommendation; the 10-min browser-test rule mechanized as a hook.
Nothing committed by the assistant beyond this session's own files (see "Do not" below).

## What shipped

### 1. Scale-template framework — one authored game → table · sidewalk · urban (auto-generated)
The reusable seam: game LOGIC is scale-agnostic, only a `ScaleProfile` swaps per scale. Pure-TS → graduates to
**RN 3JS V5 + Unity** unchanged; only the render layer is web-specific. Doc: `docs/SCALE_TEMPLATE.md`.

- **Game #1 "Blades"** (🟢 built) — cyberpunk racer: audio-reactive neon city (buildings = frequency bars),
  fly through Portals to score, + a "Generate your own game" composer (prompt/image/YouTube) & walkthrough below it.
  - Seams: `src/lib/scale/gameTemplate.ts` (9/9), `src/lib/scale/cityGen.ts` (7/7). Render: `src/addons/scale-worlds/ScaleWorldsModule.tsx`. Tab: **Blades**.
- **Game #2 "Archetype Hunt"** (🟢 built increment 1) — hunt the 16 generative archetypes (spiral, L-system,
  Mandelbrot, Game-of-Life, WFC, DNA, Voronoi, Turing, …) in the real world via camera, they erupt into a living
  procedural system you grow & rewire; new-discovery bonus (Shannon surprise). Spec: `docs/GAME_TEMPLATE_2_ARCHETYPE_HUNT.md`.
  - Seams: `src/lib/scale/archetypes.ts` (8/8), `src/lib/scale/promptLadder.ts` (6/6), `src/lib/scale/growSystem.ts` (7/7).
    Render: `src/addons/archetype-hunt/ArchetypeHuntModule.tsx` (deterministic MockRecognizer; real MediaPipe camera = next increment). Tab: **Hunt**.
  - **Prompt-ladder** = archetype word → small/medium/large prompts (× place × time) = the three scales. The operator's
    Spiral × East-River example is hard-coded (whirlpools → drain-vortex → water-tornado).

Wiring: `src/spatial/SpatialWebpageModule/ui/SidePanel.tsx` (2 tabs), `scripts/e2e.mjs` (+36 checks → 90).

### 2. Creator provenance & minting — research spike (3 agents, cited) + recommendation
- `docs/research/PROVENANCE_RECOMMENDATION_2026-07-10.md` ← **read this first** (synthesis + concrete `XRAI.json` schema).
- Sources: `PROVENANCE_MINTING_SOTA_2026-07-10.md` · `PROVENANCE_STANDARDS_W3C_2026-07-10.md` · `PORTALS_MINTING_BACKEND_AUDIT_2026-07-10.md`.
- **Answer:** invisible account-keyed signed provenance in `XRAI.json` NOW (zero friction, no wallet), on-chain mint
  (thirdweb, gasless) as an opt-in button LATER. Minting is **greenfield** (old coin = HMMM, being scrubbed; "Fuel"
  points ≠ coin). Only gap in the format = a `license` field. Standards: DID + SPDX + `parents[]` (PROV-O) + Data
  Integrity proof + optional C2PA. The operator's 3 tiers (private-reserved / private-CC-remix / collective) map 1:1.

### 3. Test-cadence hook (operator hard rule mechanized)
`.claude/hooks/web-test-staleness-guard.sh` + registered in `.claude/settings.json` Stop chain. Blocks Stop if any
`web/jarvis-hyperjam/src|scripts` file is newer than the last `loop-e2e.json` AND >10 min old → "run `npm test`".
Verified (fresh→0, 15-min-stale→2, restored→0). Additive; won't trip `protect-hooks` (guards removals only).

## How to run / verify
```
cd web/jarvis-hyperjam
npm test      # build (tsc+vite) → headless auto-open + auto-click e2e → must print 90/90 🟢
npm run dev   # live; tabs: Blades, Hunt (+ Jam, Vibe, Hyper Jarvis, Hologram, RGBD, Procedural, Icosa, Avatar)
```

## Next steps (in priority order)
1. **Build Phase 1 provenance** (the one missing piece): pure-TS `src/lib/scale/provenance.ts` (creator DID + SPDX
   license tier + `parents[]` + signature; self-test) + the 3-tier chooser wired into Blades & Hunt "save my creation",
   recorded in the immutable Firestore `assets/{assetId}` registry (`firestore.rules:145-150`). Reuse existing
   `XraiDoc.author`/`metadata.parents[]` (`XraiWebBridge.ts:23-27`, `shareCodec.ts:99-118`). Loop-green.
2. **Blades roadmap:** real mic/Tone.js audio + procedural instruments · urban visuals (sky-beams, profile-on-clouds,
   plex web, Doppler, bloom) · LiveKit multiplayer · hands+breath drive · horns→turbo→sonic-boom.
3. **Hunt roadmap:** real MediaPipe recognizer from the `ingest/` exemplar crawl · AR anchoring on the real object ·
   species/age metadata (labeled estimates) · story engine routing to the next unlock · Powers-of-Ten zoom.
4. **Graduate** the pure-TS seams into the RN 3JS V5 + Unity composers (same modules, render swapped).

## Do NOT / honesty notes
- **Do not sweep the ~1186 pre-existing uncommitted files** in this tree — the assistant committed ONLY this session's
  own files. The rest is the operator's WIP to commit separately.
- Camera/vision (Hunt) = **on-device only, synthetic/consented testing**; object metadata (tree age) = labeled ESTIMATE.
- Provenance/license = engineering mapping of primary standards, **not legal advice** — counsel before publishing a chooser.
- Research license landmines: Story Protocol = BSL-1.1 (not permissive); C2PA alone doesn't prove identity (needs account key); Reservoir pivoted off NFT tooling.
